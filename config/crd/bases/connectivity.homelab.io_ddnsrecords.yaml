---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: ddnsrecords.connectivity.homelab.io
spec:
  group: connectivity.homelab.io
  names:
    kind: DDNSRecord
    listKind: DDNSRecordList
    plural: ddnsrecords
    shortNames:
    - ddns
    singular: ddnsrecord
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.provider
      name: Provider
      type: string
    - jsonPath: .spec.domain
      name: Domain
      type: string
    - jsonPath: .spec.host
      name: Host
      type: string
    - jsonPath: .status.ready
      name: Ready
      type: boolean
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: DDNSRecord is the Schema for the ddnsrecords API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: DDNSRecordSpec defines the desired state of DDNSRecord
            properties:
              domain:
                description: Domain is the base domain (e.g., daddyshome.fr)
                type: string
              host:
                description: Host is the subdomain or @ for root
                type: string
              ipVersion:
                default: ipv4
                description: IPVersion specifies which IP version to update
                enum:
                - ipv4
                - ipv6
                - ipv4_or_ipv6
                type: string
              provider:
                default: ovh
                description: Provider is the DDNS provider (currently only OVH supported)
                enum:
                - ovh
                type: string
              providerConfig:
                description: ProviderConfig contains provider-specific settings
                properties:
                  credentialsRef:
                    description: |-
                      CredentialsRef references the secret containing OVH credentials
                      Secret must contain: OVH_APPLICATION_KEY, OVH_APPLICATION_SECRET, OVH_CONSUMER_KEY
                    properties:
                      name:
                        description: Name of the secret
                        type: string
                      namespace:
                        description: Namespace of the secret (defaults to the DDNSRecord
                          namespace)
                        type: string
                    required:
                    - name
                    type: object
                  mode:
                    default: api
                    description: Mode is the OVH API mode
                    type: string
                required:
                - credentialsRef
                type: object
            required:
            - domain
            - host
            - providerConfig
            type: object
          status:
            description: DDNSRecordStatus defines the observed state of DDNSRecord
            properties:
              conditions:
                description: Conditions represent the current state of the DDNSRecord
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              lastSyncedAt:
                description: LastSyncedAt is the timestamp of the last successful
                  sync
                format: date-time
                type: string
              ready:
                description: Ready indicates if the record has been synced to the
                  ConfigMap
                type: boolean
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
